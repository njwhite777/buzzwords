'use strict';

/**
 * @ngdoc function
 * @name frontendApp.controller:appscreenController
 * @description
 * # appscreenController
 * Controller of the initial period of game
 */
angular.module('frontendApp')
      .controller('appscreenController',[
        '$scope',
        '$state',
        function($scope,$state){
          console.log("AS controller.")

        }
    ]
  );
